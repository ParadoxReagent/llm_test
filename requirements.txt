# Main dependencies (for quick setup)
# For production use with pinned versions, use: pip install -r requirements-lock.txt
# Or install as a package: pip install -e .

litellm>=1.77.1,<2.0.0
python-dotenv>=1.0.0,<2.0.0
aiohttp>=3.11.10
rich>=13.7.0,<14.0.0
pyyaml>=6.0.1,<7.0.0
tenacity>=8.2.3,<9.0.0
